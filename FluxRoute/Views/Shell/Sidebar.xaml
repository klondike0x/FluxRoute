<UserControl x:Class="FluxRoute.Views.Shell.Sidebar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:shell="clr-namespace:FluxRoute.Views.Shell">
    <Border Background="#0C0F17"
            BorderThickness="0,0,1,0">
        <Border.BorderBrush>
            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                <GradientStop Color="#00FFFFFF" Offset="0" />
                <GradientStop Color="#1AFFFFFF" Offset="0.14" />
                <GradientStop Color="#1AFFFFFF" Offset="0.86" />
                <GradientStop Color="#00FFFFFF" Offset="1" />
            </LinearGradientBrush>
        </Border.BorderBrush>
        <Border.Resources>
            <Style x:Key="SidebarLabel" TargetType="TextBlock">
                <Setter Property="FontFamily" Value="Segoe UI" />
                <Setter Property="FontSize" Value="13" />
                <Setter Property="FontWeight" Value="Medium" />
                <Setter Property="Foreground" Value="#DCE4F0" />
                <Setter Property="VerticalAlignment" Value="Center" />
                <Setter Property="Margin" Value="12,0,0,0" />
                <Setter Property="Visibility" Value="Collapsed" />
                <Style.Triggers>
                    <DataTrigger Binding="{Binding IsExpanded, RelativeSource={RelativeSource AncestorType=shell:Sidebar}}" Value="True">
                        <Setter Property="Visibility" Value="Visible" />
                    </DataTrigger>
                </Style.Triggers>
            </Style>

            <Style x:Key="AdaptiveNavBtn" TargetType="Button">
                <Setter Property="Height" Value="40" />
                <Setter Property="Width" Value="52" />
                <Setter Property="Margin" Value="0,2" />
                <Setter Property="HorizontalAlignment" Value="Center" />
                <Setter Property="HorizontalContentAlignment" Value="Center" />
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Cursor" Value="Hand" />
                <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="Bg" Background="{TemplateBinding Background}"
                                    CornerRadius="8" Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  VerticalAlignment="Center" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bg" Property="Background" Value="#10FFFFFF" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <DataTrigger Binding="{Binding IsExpanded, RelativeSource={RelativeSource AncestorType=shell:Sidebar}}" Value="True">
                        <Setter Property="Width" Value="Auto" />
                        <Setter Property="HorizontalAlignment" Value="Stretch" />
                        <Setter Property="HorizontalContentAlignment" Value="Left" />
                        <Setter Property="Padding" Value="16,0" />
                        <Setter Property="Margin" Value="8,2" />
                    </DataTrigger>
                </Style.Triggers>
            </Style>

            <Style x:Key="NavIcon" TargetType="TextBlock">
                <Setter Property="FontFamily" Value="Segoe MDL2 Assets" />
                <Setter Property="FontSize" Value="18" />
                <Setter Property="Foreground" Value="#71717A" />
                <Setter Property="VerticalAlignment" Value="Center" />
            </Style>
        </Border.Resources>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,8,0,0">
                <Button Style="{StaticResource AdaptiveNavBtn}" Command="{Binding SelectTabCommand}" CommandParameter="0" ToolTip="Главная">
                    <StackPanel Orientation="Horizontal"><TextBlock Style="{StaticResource NavIcon}" Text="&#xE80F;"/><TextBlock Style="{StaticResource SidebarLabel}" Text="Главная"/></StackPanel>
                </Button>
                <Button Style="{StaticResource AdaptiveNavBtn}" Command="{Binding SelectTabCommand}" CommandParameter="1" ToolTip="Компоненты">
                    <StackPanel Orientation="Horizontal"><TextBlock Style="{StaticResource NavIcon}" Text="&#xE950;"/><TextBlock Style="{StaticResource SidebarLabel}" Text="Компоненты"/></StackPanel>
                </Button>
                <Button Style="{StaticResource AdaptiveNavBtn}" Command="{Binding SelectTabCommand}" CommandParameter="2" ToolTip="Оркестратор">
                    <StackPanel Orientation="Horizontal"><TextBlock Style="{StaticResource NavIcon}" Text="&#xE99A;"/><TextBlock Style="{StaticResource SidebarLabel}" Text="Оркестратор"/></StackPanel>
                </Button>
                <Button Style="{StaticResource AdaptiveNavBtn}" Command="{Binding SelectTabCommand}" CommandParameter="3" ToolTip="Модификации">
                    <StackPanel Orientation="Horizontal"><TextBlock Style="{StaticResource NavIcon}" Text="&#xE74C;"/><TextBlock Style="{StaticResource SidebarLabel}" Text="Модификации"/></StackPanel>
                </Button>
                <Button Style="{StaticResource AdaptiveNavBtn}" Command="{Binding SelectTabCommand}" CommandParameter="4" ToolTip="Диагностика">
                    <StackPanel Orientation="Horizontal"><TextBlock Style="{StaticResource NavIcon}" Text="&#xE90F;"/><TextBlock Style="{StaticResource SidebarLabel}" Text="Диагностика"/></StackPanel>
                </Button>
                <Button Style="{StaticResource AdaptiveNavBtn}" Command="{Binding SelectTabCommand}" CommandParameter="5" ToolTip="Логи">
                    <StackPanel Orientation="Horizontal"><TextBlock Style="{StaticResource NavIcon}" Text="&#xE9F9;"/><TextBlock Style="{StaticResource SidebarLabel}" Text="Логи"/></StackPanel>
                </Button>
            </StackPanel>

            <StackPanel Grid.Row="1" Margin="0,0,0,8">
                <Rectangle Height="1" Fill="#1E2235" Margin="8,4" />
                <Button Style="{StaticResource AdaptiveNavBtn}" Command="{Binding SelectTabCommand}" CommandParameter="6" ToolTip="Настройки">
                    <StackPanel Orientation="Horizontal"><TextBlock Style="{StaticResource NavIcon}" Text="&#xE713;"/><TextBlock Style="{StaticResource SidebarLabel}" Text="Настройки"/></StackPanel>
                </Button>
            </StackPanel>

            <Border x:Name="NavIndicatorBorder" Width="3" Height="20" CornerRadius="1.5"
                    HorizontalAlignment="Left" VerticalAlignment="Top" Margin="0,20,0,0" Grid.RowSpan="2">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Color="#BB88FF" Offset="0" />
                        <GradientStop Color="#7744EE" Offset="1" />
                    </LinearGradientBrush>
                </Border.Background>
                <Border.RenderTransform><TranslateTransform x:Name="NavIndicatorTransform" /></Border.RenderTransform>
            </Border>
        </Grid>
    </Border>
</UserControl>

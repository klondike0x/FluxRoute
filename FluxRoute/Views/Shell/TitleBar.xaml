<UserControl x:Class="FluxRoute.Views.Shell.TitleBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="40" d:DesignWidth="860"
             MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
    <Border Background="#0C0F17" CornerRadius="12,12,0,0">
        <Grid Margin="12,0">
            <!-- Logo mark -->
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Image Source="/Resources/FluxRoute.png"
                       Width="22" Height="22" Margin="0,0,9,0"
                       RenderOptions.BitmapScalingMode="HighQuality" />
                <TextBlock VerticalAlignment="Center" FontFamily="Segoe UI" FontSize="13">
                    <Run Text="FluxRoute" FontWeight="SemiBold" Foreground="#C8D4EC" />
                    <Run Text=" v" Foreground="#C8D4EC" FontWeight="SemiBold" />
                    <Run Text="{Binding AppVersion, Mode=OneWay}" Foreground="#C8D4EC" FontWeight="SemiBold" />
                    <Run Text=" by klondike0x" Foreground="#3D4260" FontWeight="Normal" />
                </TextBlock>
            </StackPanel>

            <!-- Window controls -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right"
                        VerticalAlignment="Center" Margin="0,0,-2,0">
                <Button x:Name="MinimizeBtn" Style="{StaticResource TitleBtn}"
                        Click="MinimizeButton_Click">
                    <TextBlock Text="&#xE921;" FontFamily="Segoe MDL2 Assets" FontSize="9">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="#4A5170" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsMouseOver, ElementName=MinimizeBtn}" Value="True">
                                        <Setter Property="Foreground" Value="#C0C8E8" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Button>
                <Button x:Name="CloseBtn" Style="{StaticResource CloseTitleBtn}"
                        Click="CloseButton_Click">
                    <TextBlock Text="&#xE8BB;" FontFamily="Segoe MDL2 Assets" FontSize="9">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="#4A5170" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsMouseOver, ElementName=CloseBtn}" Value="True">
                                        <Setter Property="Foreground" Value="#E8394A" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>

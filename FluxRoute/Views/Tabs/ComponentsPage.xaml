<UserControl x:Class="FluxRoute.Views.Tabs.ComponentsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:tabs="clr-namespace:FluxRoute.Views.Tabs">
    <UserControl.Resources>
        <Style x:Key="ComponentsTabItem" TargetType="TabItem">
            <Setter Property="Foreground" Value="#71717A" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="14,10,14,9" />
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="FontWeight" Value="Medium" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Grid Background="Transparent">
                            <Border x:Name="HoverBackground"
                                    Background="Transparent"
                                    CornerRadius="6,6,0,0">
                                <ContentPresenter ContentSource="Header"
                                                  Margin="{TemplateBinding Padding}"
                                                  HorizontalAlignment="Center"
                                                  VerticalAlignment="Center" />
                            </Border>
                            <Border x:Name="ActiveLine"
                                    Height="2"
                                    Background="#DCE4F0"
                                    VerticalAlignment="Bottom"
                                    Margin="10,0"
                                    Visibility="Collapsed" />
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="HoverBackground" Property="Background" Value="#0DFFFFFF" />
                                <Setter Property="Foreground" Value="#B7C3D8" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Foreground" Value="#DCE4F0" />
                                <Setter TargetName="ActiveLine" Property="Visibility" Value="Visible" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ComponentsTabControl" TargetType="TabControl">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabControl">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <Border Grid.Row="0"
                                    BorderBrush="#1E2235"
                                    BorderThickness="0,0,0,1">
                                <DockPanel LastChildFill="True">
                                    <TextBlock Text="Компоненты"
                                               Foreground="#DCE4F0"
                                               FontFamily="Segoe UI"
                                               FontSize="13"
                                               FontWeight="SemiBold"
                                               VerticalAlignment="Center"
                                               Margin="0,0,16,0" />
                                    <Border Width="1" Height="18"
                                            Background="#30363D"
                                            Margin="0,0,8,0"
                                            VerticalAlignment="Center" />
                                    <TabPanel IsItemsHost="True"
                                              KeyboardNavigation.TabIndex="1"
                                              Background="Transparent" />
                                </DockPanel>
                            </Border>

                            <Border Grid.Row="1" Background="Transparent" Padding="0,12,0,0">
                                <ContentPresenter x:Name="PART_SelectedContentHost"
                                                  ContentSource="SelectedContent" />
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="16,12">
        <TabControl Style="{StaticResource ComponentsTabControl}"
                    ItemContainerStyle="{StaticResource ComponentsTabItem}"
                    SelectedIndex="0">
            <!-- Zapret, GameFilter и IPSet -->
            <TabItem Header="Zapret">
                <tabs:ServicePage />
            </TabItem>

            <!-- Telegram WebSocket Proxy -->
            <TabItem Header="TgWsProxy">
                <tabs:TgProxyPage />
            </TabItem>

            <!-- DNS-over-HTTPS -->
            <TabItem Header="DoH">
                <Grid Margin="0,4,10,12">
                    <Border Background="#111520"
                            BorderBrush="#21262D"
                            BorderThickness="1"
                            CornerRadius="8"
                            Padding="20"
                            VerticalAlignment="Top">
                        <StackPanel>
                            <TextBlock Text="DNS-over-HTTPS"
                                       Foreground="#DCE4F0"
                                       FontSize="15"
                                       FontWeight="SemiBold" />
                            <TextBlock Text="Компонент DoH будет доступен после интеграции ветки feature/doh."
                                       Foreground="#8B949E"
                                       FontSize="12"
                                       TextWrapping="Wrap"
                                       Margin="0,8,0,12" />
                            <Border Background="#1C1800"
                                    BorderBrush="#4A3800"
                                    BorderThickness="1"
                                    CornerRadius="7"
                                    Padding="10,8">
                                <TextBlock Text="Ожидает интеграции"
                                           Foreground="#F0B429"
                                           FontSize="11" />
                            </Border>
                        </StackPanel>
                    </Border>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
